# Tools

Add stable custom Agent Builder tool specs here after real discovery.

Recommended order:

1. Narrow index search tools for confirmed dataset families.
2. Small ES|QL aggregation tools with explicit `LIMIT`.
3. Workflow tools only when an agent needs to trigger actions.

Avoid broad catch-all tools. The more focused the tool description and scope, the more reliable the agent will be.

